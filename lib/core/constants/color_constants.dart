import 'package:flutter/material.dart';

/// Constantes de colores utilizadas en toda la aplicación
/// 
/// Este archivo centraliza todos los colores personalizados para
/// mantener consistencia visual y facilitar cambios de tema
class AppColors {
  // Prevenir instanciación
  AppColors._();

  // Colores de fondo
  /// Color de fondo oscuro principal usado en cards, containers y superficies
  static const Color darkBackground = Color(0xFF2A2A2A);
  
  /// Color de fondo oscuro alternativo
  static const Color darkBackgroundAlt = Color(0xFF1E1E1E);

  // Agregar aquí más colores según sea necesario
  // Por ejemplo:
  // static const Color primary = Color(0xFF...);
  // static const Color secondary = Color(0xFF...);
  // static const Color accent = Color(0xFF...);
}
